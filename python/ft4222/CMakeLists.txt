# Copyright 2011 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of ft4222
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Include python install macros
########################################################################
include(GrPython)
if(NOT PYTHONINTERP_FOUND)
    return()
endif()

########################################################################
# Check for pyft4222
########################################################################
# GR_PYTHON_CHECK_MODULE_RAW(
#     "pyft4222"
#     "import pyft4222"
#     PYFT4222_FOUND
#     )

set(GR_PYFT4222_MIN_VERSION "0.2.0")

gr_python_check_module(
    "pyft4222 >= ${GR_PYFT4222_MIN_VERSION}" pyft4222
    "LooseVersion(pyft4222.__version__) >= LooseVersion('${GR_PYFT4222_MIN_VERSION}')" PYFT4222_FOUND)

if(NOT PYFT4222_FOUND)
    message(ERROR "The python package pyft4222 is missing"
                  "Please run pip3 install pyft4222")
endif(NOT PYFT4222_FOUND)

add_subdirectory(bindings)

########################################################################
# Install python sources
########################################################################
gr_python_install(FILES __init__.py pyft4222_source.py py.typed DESTINATION ${GR_PYTHON_DIR}/gnuradio/ft4222)

########################################################################
# Handle the unit tests
########################################################################
include(GrTest)

set(GR_TEST_TARGET_DEPS gnuradio-ft4222)

# Create a package directory that tests can import. It includes everything
# from `python/`.
add_custom_target(
    copy_module_for_tests ALL
    COMMAND ${CMAKE_COMMAND} -E copy_directory ${CMAKE_CURRENT_SOURCE_DIR}
            ${PROJECT_BINARY_DIR}/test_modules/gnuradio/ft4222/)
GR_ADD_TEST(qa_pyft4222_source ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/qa_pyft4222_source.py)
